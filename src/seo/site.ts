/** Single canonical host for URLs, sitemap, JSON-LD, and redirects. */
export const CANONICAL_HOST = "www.snaptagtrack.com" as const;
export const CANONICAL_ORIGIN = `https://${CANONICAL_HOST}` as const;
